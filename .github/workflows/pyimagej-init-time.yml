name: Measure PyImageJ Init Time

on:
  workflow_dispatch:

jobs:
  test-pyimagej:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install micromamba
        uses: mamba-org/setup-micromamba@v1
        with:
          micromamba-version: "latest"
          environment-name: test-env
          create-args: >-
            python=3.10
          init-shell: bash
          cache-downloads: false
          cache-environment: false

      - name: Install pyimagej (no cache)
        run: micromamba install -y -n test-env -c conda-forge pyimagej openjdk=11

      - name: Run PyImageJ test with timing
        run: micromamba run -n test-env time python -c "import imagej; ij = imagej.init('2.14.0'); print(ij.getVersion())"

      - name: Run Fiji test with timing
        run: micromamba run -n test-env time python -c "import imagej; ij = imagej.init('sc.fiji:fiji:2.14.0'); print(ij.getVersion())"
